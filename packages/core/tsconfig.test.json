{
  // The suite runs on Node and may say so. Kept apart from tsconfig.json so
  // that permission never reaches src/.
  "extends": "../../tsconfig.base.json",
  "compilerOptions": { "types": ["node"] },
  "include": ["src", "test"]
}
